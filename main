import java.util.*; 

void setup() {
  
  size (1000,800);
  background (255); 
  
  readData(); // read file

}

void draw () {
  
  
  
} 


void readData () {
  
       String[] data = loadStrings("flights_full.csv"); 
       for (int i = 0; i < data.length; i++) {  
          System.out.println (data[i]);
       }
       
  }
